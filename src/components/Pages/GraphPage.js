import React from 'react';

const GraphPage = () => {
    return (
        <div style={ {padding: "20px"} }>
            <h1 style={ {color: "red"} }>Графики в данный момент не поддерживаются</h1>
        </div>
    );
};

export default GraphPage;